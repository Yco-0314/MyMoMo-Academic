# BHW Informational Cascade — review

**tier: minimal** (all-REPRO 3/3; every empirical constant matches its BHW closed form within
the locked tolerance; no load-bearing control / dead-mechanism risk). Genuine agent-based.

## Mandatory cheap checks (all pass)
- numeric-provenance: **pass** — headline + graded numbers match verdict-bundle/results (P1 1.6e-4≈1.71e-4, P2 0.154≈0.155, P3 3.46≈3.45).
- fair-control: **n/a** — no two-arm contrast; each clause is a closed-form vs Monte-Carlo match.
- no-post-lock-drift: **pass** — graded verbatim against the committed lock (the CORRECTED follow-own-signal convention); L3 integrity gate green. The builder fixed its OWN test assertion (had expected 0.1552; the lock's clause is the social-accuracy gain 0.145) — a test correction to match the lock, not a verdict/lock tune.
- mechanism-aliveness: **pass** — the Bayes ±2 barrier + herding is exercised; the ±1-walk-on-d reduction reproduces all three closed forms.
- framing-disclosure: **pass** — genuine sequential Bayesian agents, disclosed.

## Verdict: SOUND. 3/3 REPRO, closed-form-exact.
Faithful implementation of the standard (follow-own-signal) BHW model; this vindicates the pre-build
lock correction (the first verification had used a non-standard coin-flip formula 0.753@p=.7; the
committed lock and this build use the canonical (1−p)²/(p²+(1−p)²)=0.155@p=.7, matched to ±0.001).
23 tests. No tuning, no fabrication.

REVIEW COMPLETE
