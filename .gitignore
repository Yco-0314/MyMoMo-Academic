# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Runtime outputs
workspace/
*.db
*.sqlite

# Transient benchmark scorecards (regenerated each run)
benchmark_calibration_summary.md

# Pipeline-stability benchmark artifacts (transient — regenerated by benchmark_pipeline_stability.py)
_pipeline_stability_run_*.log
benchmark_pipeline_stability.py
benchmark_pipeline_stability_summary.md

# Per-example handcrafted model outputs (re-emitted by main.py on every run)
examples/*/handcrafted_model/data/output/

# Adversarial codegen story.md + README.md are committed (first-class
# fixture for the anti-pattern validator). Only ignore the data/output/
# scratch produced when a dogfood writes results into the example dir.
examples/adversarial_codegen/data/output/

# In-flight design docs (not yet ADR'd; kept locally during drafting)
docs/mymomo-simulation-engine-evolution-plan.md
docs/mymomo-world-model-roadmap.md

# macOS
.DS_Store

# Secrets
.env

# Claude Code
.claude/
examples/calibration_challenge_virus/handcrafted_model/data/output/
